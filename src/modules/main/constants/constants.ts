import {IDateTimeFormat} from '../types/main.module.types.ts';

const DateTimeFormat: IDateTimeFormat = {
    DATE_TIME_FORMAT: 'dd/MM/yyyy hh:mm aaa',
    DATE_FORMAT: 'dd/MM/yyyy',
    TIME_FORMAT: 'hh:mm aaa',
};

Object.freeze(DateTimeFormat);

export default DateTimeFormat;
